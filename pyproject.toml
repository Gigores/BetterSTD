[project]
name = "btrstd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "metapy",
]

[tool.uv.sources]
metapy = { git = "https://codeberg.org/Priklen/MetaPy.git" }
